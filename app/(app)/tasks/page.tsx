import { ComingSoon } from "@/components/layout/coming-soon";

export default function TasksPage() {
  return (
    <ComingSoon
      title="Tasks"
      description="Task lists, filters, and project links land in a later build phase."
    />
  );
}
